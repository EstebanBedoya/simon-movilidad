# ============================================================
# Simon Movilidad — Infra Environment Variables
# Copy to .env and fill in values before running docker-compose
# ============================================================

# ------ PostgreSQL ------
DB_HOST=localhost
DB_PORT=5432
DB_USER=simon
DB_PASSWORD=simon123
DB_NAME=simon_movilidad

# ------ JWT (manual crypto implementation) ------
JWT_SECRET=supersecretkey_change_in_production
JWT_EXPIRES_IN=86400        # seconds (24h)

# ------ Simulation ------
SIMULATE=true
SIMULATE_INTERVAL_MS=3000   # ms between telemetry ticks

# ------ App ------
PORT=3001
NODE_ENV=development
